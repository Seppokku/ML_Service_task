This directory stores raw datasets for training runs.

For this project, the default training dataset is:
- `data/raw/bbc-text.csv`

Expected columns:
- `text` (string)
- `category` (string label)

You can point training to another file via `DATA_PATH` in `.env`.
